import React from "react";

import "./Login.css"

function Login({ setToken }) {
    //Login na dashboard, caso ok redireciona pra dashboard/main (utilizar setToken para realizar o login), caso contrário continua aqui

    <main>
        <div className="dashboard__login"><p color="#FFF">Teste</p></div>
    </main>
}

export default Login;